@model Inventory.Web.ViewModels.TransferVm
@{
    ViewData["Title"] = "Transfer";
}

<div class="page-header">
    <h1 class="page-title">Transfer to store</h1>
    <p class="page-subtitle">Move units from the warehouse to a retail store.</p>
</div>

@if (TempData["Success"] is string successMessage)
{
    <div class="alert alert-success mb-4" role="status">@successMessage</div>
}

<div class="surface-card form-panel">
    <div class="card-inner">
        <form method="post">
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

            <div class="mb-3">
                <label asp-for="ProductId" class="form-label"></label>
                <select asp-for="ProductId" asp-items="Model.Products" class="form-select"></select>
                <span asp-validation-for="ProductId" class="text-danger small"></span>
            </div>

            <div class="mb-3">
                <label asp-for="ToStoreId" class="form-label"></label>
                <select asp-for="ToStoreId" asp-items="Model.Stores" class="form-select"></select>
                <span asp-validation-for="ToStoreId" class="text-danger small"></span>
            </div>

            <div class="mb-0">
                <label asp-for="Quantity" class="form-label"></label>
                <input asp-for="Quantity" class="form-control" inputmode="numeric" />
                <span asp-validation-for="Quantity" class="text-danger small"></span>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Submit transfer</button>
            </div>
        </form>
    </div>
</div>
